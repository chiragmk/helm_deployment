pipeline {
    agent any
    stages {
        stage('Deploy') {
            steps {
                

                timeout(time: 2, unit: 'MINUTES') {
                    sh 'echo done'
                }
            }
        }
    }
}
